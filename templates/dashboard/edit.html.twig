{% extends 'base.html.twig' %}
{% block body %}
    <edit-site :site="{{ site.serializer('json') }}"
               form-url="{{ url('update_site_json') }}"
               csrf-token="{{ csrf_token('save-site') }}"
               id="{{ site.id }}"
               redirect-url="{{ url('index') }}"
    ></edit-site>
{% endblock %}
